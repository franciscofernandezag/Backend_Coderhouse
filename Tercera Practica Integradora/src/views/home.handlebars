<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
  <title>Página de Login y Registro</title>
</head>

<body>

  <div class="container">
    <div class="row">
      <div class="col-md-6">
        <h3>Iniciar Sesión</h3>
        {{#if error}}
        <div class="alert alert-danger" role="alert">
          {{error}}
        </div>
        {{/if}}
        {{#if success}}
        <div class="alert alert-success">{{success}}</div>
        {{/if}}
        {{#if error}}
        <div class="alert alert-danger">{{error}}</div>
        {{/if}}
        <form action="/login" method="post">
          <div class="form-group">
            <label for="login-email">Email:</label>
            <input type="email" class="form-control" id="login-email" name="email" placeholder="Ingrese su email" />
          </div>
          <div class="form-group">
            <label for="login-password">Contraseña:</label>
            <input type="password" class="form-control" id="login-password" name="password"
              placeholder="Ingrese su contraseña" />
          </div>
          <button type="submit" class="btn btn-primary">Iniciar Sesión</button>

          <p>
            <a href="#" id="forgot-password-link">No recuerda contraseña</a>
          </p>

        </form>

      <div>
        <div class="pt-4">
          <h4>Iniciar Sesión con GitHub</h4>
          <a href="/auth/github" class="btn btn-secondary">Iniciar Sesión con GitHub</a>
        </div>
      </div>

      </div>
      <div class="col-md-6">
        <h3>Registrarse</h3>
        <form action="/registro" method="post">
          <div class="form-group">
            <label for="signup-nombre">Nombre:</label>
            <input type="text" class="form-control" id="signup-nombre" name="nombre" placeholder="Ingrese su nombre" />
          </div>
          <div class="form-group">
            <label for="signup-apellido">Apellido:</label>
            <input type="text" class="form-control" id="signup-apellido" name="apellido"
              placeholder="Ingrese su apellido" />
          </div>
          <div class="form-group">
            <label for="signup-email">Email:</label>
            <input type="email" class="form-control" id="signup-email" name="email" placeholder="Ingrese su email" />
          </div>
          <div class="form-group">
            <label for="signup-edad">Edad:</label>
            <input type="number" class="form-control" id="signup-edad" name="edad" placeholder="Ingrese su edad" />
          </div>
          <div class="form-group">
            <label for="signup-genero">Género:</label>
            <select class="form-control" id="signup-genero" name="genero">
              <option value="masculino">Masculino</option>
              <option value="femenino">Femenino</option>
              <option value="indeterminado">Indeterminado</option>
            </select>
          </div>
          <div class="form-group">
            <label for="signup-rol">Rol:</label>
            <select class="form-control" id="signup-rol" name="rol">
              <option value="usuario">Usuario</option>
              <option value="administrador">Administrador</option>
            </select>
          </div>
          <div class="form-group">
            <label for="signup-password">Contraseña:</label>
            <input type="password" class="form-control" id="signup-password" name="password"
              placeholder="Ingrese su contraseña" />
          </div>
          <button type="submit" class="btn btn-primary">Registrarse</button>
        </form>
      </div>
</div>
      <div id="forgot-password-form" style="display: none;">
        <div class="row mt-5">
          <div class="col-md-6">
            <h5>Recuperar Contraseña</h5>
            <form action="/recoverypassword" method="post">
              <div class="form-group">
                <label for="forgot-password-email">Email:</label>
                <input type="email" class="form-control" id="forgot-password-email" name="email"
                  placeholder="Ingrese su email registrado" />
              </div>
              <p>Ingresa tu E-mail y te enviaremos un correo electrónico para cambiar tu contraseña</p>
              <button type="submit" class="btn btn-sm btn-primary">Enviar</button>
            </form>
          </div>
        </div>
      </div>
    </div>

      
    

    

    <script>
      // JavaScript para mostrar/ocultar el formulario de recuperación de contraseña
      const forgotPasswordLink = document.getElementById("forgot-password-link");
      const forgotPasswordForm = document.getElementById("forgot-password-form");
      forgotPasswordLink.addEventListener("click", function (event) {
        event.preventDefault();
        if (forgotPasswordForm.style.display === "none") {
          forgotPasswordForm.style.display = "block";
        } else {
          forgotPasswordForm.style.display = "none";
        }
      });
    </script>

</body>

</html>